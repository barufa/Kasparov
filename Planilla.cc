#include <bits/stdc++.h>
using namespace std;

typedef long long int ll;
typedef unsigned long long int ull;
typedef long double ld;
#define forr(i, a, b)  for (ll(i) = (ll) (a); (i) < (ll) (b); (i)++)
#define forn(i, n)     forr(i, 0, n)
#define forall(it, v)  for (auto it = v.begin(); it != v.end(); ++it)
#define forit(i, v)    for (auto& i : v)
#define rforr(i, a, b) for (ll(i) = ((ll) (b) - 1); (i) >= (ll) (a); (i)--)
#define rforn(i, n)    rforr(i, 0, n)
#define rforall(it, v) for (auto it = v.rbegin(); it != v.rend(); ++it)
#define zero(v)        memset((v), 0, sizeof(v))
#define fst     first
#define snd     second
#define set_cin ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define MAXN

int
main(void)
{
    set_cin;

    return 0;
}

// ~ Compilar: g++ -Wall -c "%f" -lm -std=c++11 -DACM -g
// ~ Construir: g++ -Wall -o "%e" "%f" -lm -std=c++11 -DACM -g
// ~ Ejecutar: time "./%e"
